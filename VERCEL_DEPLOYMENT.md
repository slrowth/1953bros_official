# Vercel 배포 가이드

이 문서는 Next.js 프로젝트를 Vercel에 배포하는 방법을 단계별로 설명합니다.

## 📋 목차

1. [사전 준비사항](#1-사전-준비사항)
2. [Vercel 계정 생성 및 프로젝트 연결](#2-vercel-계정-생성-및-프로젝트-연결)
3. [환경 변수 설정](#3-환경-변수-설정)
4. [배포 실행](#4-배포-실행)
5. [배포 확인 및 문제 해결](#5-배포-확인-및-문제-해결)

---

## 1. 사전 준비사항

### 1.1 Git 저장소 준비

Vercel은 Git 저장소와 연동하여 배포합니다. 프로젝트가 Git 저장소에 푸시되어 있어야 합니다.

```bash
# Git 저장소가 없다면 초기화
git init

# 원격 저장소 추가 (GitHub, GitLab, Bitbucket 등)
git remote add origin <your-repository-url>

# 변경사항 커밋 및 푸시
git add .
git commit -m "Initial commit"
git push -u origin main
```

### 1.2 로컬 빌드 테스트

배포 전에 로컬에서 빌드가 성공하는지 확인합니다.

```bash
# 의존성 설치
npm install

# 프로덕션 빌드 테스트
npm run build

# 빌드 성공 시 로컬 서버 실행 테스트
npm start
```

---

## 2. Vercel 계정 생성 및 프로젝트 연결

### 2.1 Vercel 계정 생성

1. [Vercel](https://vercel.com)에 접속합니다.
2. **Sign Up** 버튼을 클릭하여 계정을 생성합니다.
   - GitHub, GitLab, Bitbucket 계정으로 간편 가입 가능
   - 또는 이메일로 가입 가능

### 2.2 프로젝트 Import

1. Vercel 대시보드에서 **Add New...** > **Project**를 클릭합니다.
2. Git 저장소를 선택합니다 (GitHub, GitLab, Bitbucket 등).
3. 저장소 목록에서 프로젝트를 선택합니다.
4. **Import** 버튼을 클릭합니다.

### 2.3 프로젝트 설정

프로젝트 Import 시 다음 설정을 확인합니다:

- **Framework Preset**: Next.js (자동 감지됨)
- **Root Directory**: `./` (기본값)
- **Build Command**: `npm run build` (기본값)
- **Output Directory**: `.next` (기본값, Next.js가 자동 설정)
- **Install Command**: `npm install` (기본값)

**참고**: Next.js 프로젝트는 대부분 자동으로 설정이 감지되므로 기본값을 그대로 사용하면 됩니다.

---

## 3. 환경 변수 설정

### 3.1 필요한 환경 변수

프로젝트에서 사용하는 환경 변수는 다음과 같습니다:

- `NEXT_PUBLIC_SUPABASE_URL`: Supabase 프로젝트 URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: Supabase Anon Key (공개 키)
- `SUPABASE_SERVICE_ROLE_KEY`: Supabase Service Role Key (서버 전용, 비공개)

### 3.2 Vercel에 환경 변수 추가

1. Vercel 프로젝트 대시보드에서 **Settings** 탭으로 이동합니다.
2. 왼쪽 메뉴에서 **Environment Variables**를 클릭합니다.
3. 각 환경 변수를 추가합니다:

   **환경 변수 추가 방법:**
   - **Key**: `NEXT_PUBLIC_SUPABASE_URL`
   - **Value**: Supabase 프로젝트 URL (예: `https://xxxxx.supabase.co`)
   - **Environment**: Production, Preview, Development 모두 선택
   - **Add** 버튼 클릭

   동일한 방법으로 다음 변수들도 추가:
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY` (⚠️ Production과 Preview만 선택, Development는 선택 안 해도 됨)

### 3.3 환경 변수 확인

모든 환경 변수가 추가되었는지 확인합니다:

```
✅ NEXT_PUBLIC_SUPABASE_URL
✅ NEXT_PUBLIC_SUPABASE_ANON_KEY
✅ SUPABASE_SERVICE_ROLE_KEY
```

**참고**: 환경 변수를 추가한 후에는 새 배포를 실행해야 적용됩니다.

---

## 4. 배포 실행

### 4.1 자동 배포 (권장)

Vercel은 Git 저장소와 연동되어 자동으로 배포합니다:

1. **첫 배포**: 프로젝트 Import 시 자동으로 배포가 시작됩니다.
2. **이후 배포**: `main` 브랜치에 푸시할 때마다 자동으로 배포됩니다.
3. **Pull Request**: PR 생성 시 Preview 배포가 자동으로 생성됩니다.

### 4.2 수동 배포

수동으로 배포를 실행하려면:

1. Vercel 대시보드에서 **Deployments** 탭으로 이동합니다.
2. **Redeploy** 버튼을 클릭합니다.
3. 또는 Vercel CLI를 사용:

```bash
# Vercel CLI 설치
npm i -g vercel

# 프로젝트 디렉토리에서 배포
vercel

# 프로덕션 배포
vercel --prod
```

### 4.3 배포 진행 상황 확인

1. Vercel 대시보드의 **Deployments** 탭에서 배포 진행 상황을 확인할 수 있습니다.
2. 배포 로그를 클릭하여 상세한 빌드 로그를 확인할 수 있습니다.
3. 빌드가 완료되면 배포 URL이 생성됩니다.

---

## 5. 배포 확인 및 문제 해결

### 5.1 배포 성공 확인

배포가 완료되면:

1. Vercel 대시보드에서 배포 상태가 **Ready**로 표시되는지 확인합니다.
2. 배포 URL을 클릭하여 사이트가 정상적으로 로드되는지 확인합니다.
3. 주요 기능들이 정상 작동하는지 테스트합니다:
   - 로그인/회원가입
   - 데이터베이스 연결
   - API 엔드포인트

### 5.2 커스텀 도메인 설정 (선택사항)

1. Vercel 프로젝트 대시보드에서 **Settings** > **Domains**로 이동합니다.
2. 원하는 도메인을 입력하고 **Add** 버튼을 클릭합니다.
3. DNS 설정 가이드를 따라 도메인을 연결합니다.

### 5.3 일반적인 문제 해결

#### 빌드 실패

**증상**: 배포가 실패하고 빌드 에러가 발생합니다.

**해결 방법:**
1. 로컬에서 `npm run build`를 실행하여 에러를 확인합니다.
2. 빌드 로그를 확인하여 구체적인 에러 메시지를 확인합니다.
3. 환경 변수가 제대로 설정되었는지 확인합니다.
4. `package.json`의 빌드 스크립트가 올바른지 확인합니다.

#### 환경 변수 누락

**증상**: 배포는 성공하지만 사이트에서 API 호출이 실패합니다.

**해결 방법:**
1. Vercel 대시보드에서 **Settings** > **Environment Variables**로 이동합니다.
2. 모든 필요한 환경 변수가 추가되었는지 확인합니다.
3. 환경 변수 값이 올바른지 확인합니다.
4. 환경 변수를 추가/수정한 후 **Redeploy**를 실행합니다.

#### Supabase 연결 오류

**증상**: Supabase 관련 기능이 작동하지 않습니다.

**해결 방법:**
1. `NEXT_PUBLIC_SUPABASE_URL`과 `NEXT_PUBLIC_SUPABASE_ANON_KEY`가 올바르게 설정되었는지 확인합니다.
2. Supabase 대시보드에서 프로젝트가 활성화되어 있는지 확인합니다.
3. Supabase의 RLS (Row Level Security) 정책이 올바르게 설정되었는지 확인합니다.
4. 브라우저 콘솔에서 네트워크 에러를 확인합니다.

### 5.4 배포 로그 확인

배포 중 문제가 발생하면:

1. Vercel 대시보드의 **Deployments** 탭에서 해당 배포를 클릭합니다.
2. **Build Logs**를 확인하여 에러 메시지를 확인합니다.
3. **Function Logs**를 확인하여 런타임 에러를 확인합니다.

---

## 6. 추가 설정 (선택사항)

### 6.1 Vercel CLI 사용

로컬에서 Vercel CLI를 사용하여 배포할 수 있습니다:

```bash
# Vercel CLI 설치
npm i -g vercel

# 프로젝트 디렉토리에서 로그인
vercel login

# 프로젝트 연결
vercel link

# 개발 환경 배포
vercel

# 프로덕션 배포
vercel --prod
```

### 6.2 환경별 설정

Vercel에서는 환경별로 다른 환경 변수를 설정할 수 있습니다:

- **Production**: 프로덕션 환경 (main 브랜치)
- **Preview**: PR 및 브랜치별 미리보기
- **Development**: 로컬 개발 환경

각 환경에 맞는 환경 변수를 설정할 수 있습니다.

### 6.3 빌드 최적화

Next.js 프로젝트는 Vercel에서 자동으로 최적화됩니다:

- 자동 코드 스플리팅
- 이미지 최적화
- 정적 파일 캐싱
- Edge Functions 지원

추가 최적화가 필요하면 `next.config.mjs`에서 설정할 수 있습니다.

---

## 7. 유용한 링크

- [Vercel 공식 문서](https://vercel.com/docs)
- [Next.js 배포 가이드](https://nextjs.org/docs/app/building-your-application/deploying)
- [Vercel CLI 문서](https://vercel.com/docs/cli)
- [Supabase 문서](https://supabase.com/docs)

---

## 📝 체크리스트

배포 전 확인사항:

- [ ] Git 저장소에 코드가 푸시되어 있음
- [ ] 로컬에서 `npm run build`가 성공함
- [ ] Vercel 계정이 생성되어 있음
- [ ] Vercel에 프로젝트가 Import되어 있음
- [ ] 모든 환경 변수가 Vercel에 추가되어 있음
- [ ] Supabase 프로젝트가 활성화되어 있음
- [ ] 첫 배포가 완료되고 사이트가 정상 작동함

---

**배포 완료 후**: 배포 URL을 확인하고 주요 기능들을 테스트하여 모든 것이 정상 작동하는지 확인하세요!
